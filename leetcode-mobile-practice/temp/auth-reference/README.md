# Auth reference (your previous working implementation)

Peek here when stuck. Rebuild the live app under `app/`, `lib/`, `features/` yourself.

Suggested rebuild order:
1. lib/supabase.ts
2. features/auth/AuthProvider.tsx
3. Wire app/_layout.tsx
4. Route groups (auth) / (app)
5. features/auth/signInWithGoogle.ts + sign-in button
