# leetcode-mobile

Expo (SDK 54) + Supabase Google OAuth (browser flow).

## Get started

```bash
npm install
npx expo start
```

Requires `.env` with `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_KEY`.
